int printf(char *format, ...);
/*If recive data won't change only use the data at the time we use 'const'
    int printf(const char *fomat, ...); 
*/
int main()
{
    
    printf("siva pullaiah");

    return 0;
}
